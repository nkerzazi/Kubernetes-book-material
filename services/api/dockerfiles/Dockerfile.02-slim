# Etape 2 — image de base allegee. Agit sur la TAILLE, peu sur le temps.
FROM python:3.12-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
EXPOSE 8000
CMD ["uvicorn", "escale_api.main:app", "--host", "0.0.0.0", "--port", "8000"]
